//try { require('source-map-support').install(); } catch (e) {}

const Loader = require('@denali-js/loader').default;

module.exports = function bundle() {

  const loader = new Loader('<%= pkgName %>', '<%= version %>');
